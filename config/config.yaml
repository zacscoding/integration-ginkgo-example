server:
  port: 3000

database:
  endpoint: "root:password@(localhost)/test_db?parseTime=True&charset=utf8mb4&loc=Local"
  pool:
    maxOpen: 50
    maxIdle: 5
    maxLifeTime: 86400
  createTable: true